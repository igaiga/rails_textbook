rails_text_gemfile = File.expand_path("yasslab/Gemfile", __dir__)
if File.exist? rails_text_gemfile
  instance_eval File.read rails_text_gemfile
  return
end

source "https://rubygems.org"

gem "jekyll"
# For jekyll extension
gem 'redcarpet'
gem 'pygments.rb'

# For Github pages
gem "github-pages"
