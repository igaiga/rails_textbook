%%%
%%% タイトルページ（大扉）のデザイン
%%%

\usepackage{afterpage}                  % for '\afterpage'
\usepackage[pagecolor=none]{pagecolor}  % for '\newpagecolor'

%% 背景色を指定
%\definecolor{starter@titlepagecolor}{gray}{0.90}  % 薄いグレー
\colorlet{starter@titlepagecolor}{white}           % 白

\newcommand{\mytitlepage}{%
  \newpagecolor{starter@titlepagecolor} % このページの背景色を指定する
  \afterpage{\restorepagecolor}         % 次のページからは背景色をなくす

  \thispagestyle{empty}% ページ番号やヘッダーを入れない
  \centering           % 中央寄せ
  %\raggedright        % 左寄せ
  %\raggedleft         % 右寄せ
  %\addtolength{\oddsidemargin}{-2zw}% 右余白を調整する（右寄せの場合）

  \vspace*{5zw}        % 縦方向のスペース（ページの先頭なので「*」が必要）

  %% フォントを変更
  \gtfamily            % ゴシック体（日本語）
  \sffamily            % サンセリフ体（英語）

  %% タイトル
  {
    \HUGE              % フォントサイズをかなり大きく
    \bfseries          % 太字（日本語と英語）
    \ebseries          % 極太字（日本語のみ）
    \@title            % タイトル（複数行可）
    \par               % 段落を区切る（デザイン崩れを防ぐため）
  }

  %% サブタイトル
  \ifx\@subtitle\empty\else
    \vspace{1zw}       % 縦方向に少しスペースを空ける
    {
      \Large           % フォントサイズを大きめに
      {\rmfamily ---}~ % ローマン体で「--」
      \@subtitle       % サブタイトル
      ~{\rmfamily ---} % ローマン体で「--」
      \par             % 段落を区切る（デザイン崩れを防ぐため）
    }
  \fi

  \vspace{4zw}         % 縦方向のスペース

  %% フォントを変更
  \gtfamily            % ゴシック体（日本語）
  \sffamily            % サンセリフ体（英語）
  %% \mcfamily            % 明朝体（日本語）
  %% \rmfamily            % ローマン体（英語）

  %% 著者
  \ifx\@author\empty\else
    \vspace{24zw}       % 縦方向のスペース
    {
      \LARGE           % フォントサイズを大きく
      %\@author         % 著者
      五十嵐邦明
      \par             % 段落を区切る（デザイン崩れを防ぐため）
      ガーネットテック373株式会社
      \par             % 段落を区切る（デザイン崩れを防ぐため）
      2020年2月29日
      \par             % 段落を区切る（デザイン崩れを防ぐため）
    }
  \fi

  %% 日付、バージョン、イベント名
  %% \vfill               % 縦方向のスペース
  %% {
  %%   \Large             % フォントサイズを大きめに
    %% \ifx\@pubevent\empty\else
    %%   \@pubevent\par   % イベント名（config.ymlの「pubevent_name:」で指定）
    %% \fi
    %% \ifx\@latestpubhistory\empty
    %%   \@date           % 日付（config.ymlの「date:」で指定）
    %% \else
    %%   \@latestpubhistory % 日付とバージョン（config.ymlの「history:」で指定）
    %% \fi
  %%   \par               % 段落を区切る（デザイン崩れを防ぐため）
  %% }

  \vspace{4zw}         % 縦方向のスペース

}

%%%
%%% タイトルページ（大扉）の、次のページ
%%%
\newcommand{\mytitlenextpage}{{%
  \thispagestyle{empty}       % ページ番号やヘッダーをなくす
  \setlength{\parindent}{0pt} % 段落先頭のインデントをなくす
  \vspace*{\fill}      % 縦方向の長いスペース（ページ先頭なので「*」が必要）
  \small               % フォントを小さく

  {\gtfamily\bfseries ■ 技術書典8 まえがき}\\

本書は2012年4月から始めた一橋大学での講義資料からはじまり、そのあと幾度かの改訂を経て、今回初めての印刷製本版となりました。当時はRuby1.9, Rails3.2という環境でしたので、現在のRuby2.7, Rails6.0までの道のりで、Rubyは8つ、Railsは7つのバージョンアップを重ねたことになります。8年前から同じ言語、同じフレームワークを使って仕事ができていることは本当にすごいことです。しかもそのフレームワークは今でもバージョンアップのたびに便利になっていることも驚異的です。RubyとRailsのおかげでたのしく暮らしています。全てのRubyistのみなさんへ感謝します。\\

印刷製本版の組版にはRe:VIEW Starterを利用しています。カウプランさんには設定方法についていろいろと教えていただきました。良い感じに組版することができました。深く感謝します。\\

  {\gtfamily\bfseries ■ 著: 五十嵐邦明}\\

ガーネットテック373株式会社\\
twitter: igaiga555\\
https://igarashikuniaki.net\\

群馬高専出身。現在はフリーランスでRailsアプリ開発や技術顧問、研修設計などに携わる。大学の非常勤講師、企業の新人研修、島根県Ruby合宿、Rails寺子屋、RailsGirlsなどで講師を務める。著書に『ゼロからわかるRuby超入門』、『RubyとRailsの学習ガイド』 ほか。\\

  {\gtfamily\bfseries ■ デザイン: becolomochi}\\

べころもち工房\\
twitter: becolomochi\\
https://becolomochi.com\\

群馬高専卒業後、印刷会社にDTPオペレータとして入社。そののち関連企業のWebシステム開発部署へWebデザイナーとして異動。現在フリーランス。\\

「暖かくて優しい、しなやかなコミュニケーションを」をモットーに、イメージキャラクター制作、イラスト、デザイン、コーディング、フロントエンド実装を行なっている。群馬で異業種の夫と子と3人暮らし。ダム好き。\\

  %%% 挿絵を入れる場合
  %\includegraphics[width=0.7\textwidth]{images/illust.png}
}}
